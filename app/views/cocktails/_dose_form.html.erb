<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, placeholder: "150 ml" %>
  <%= f.input :ingredient_id, collection: Ingredient.all, prompt: "choose one" %>
  <%= f.submit "add a dose", class: "btn btn-primary btn-sm" %>
<% end %>
